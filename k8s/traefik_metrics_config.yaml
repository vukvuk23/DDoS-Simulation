apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |- # ukljucuje  traefikov ugradjeni prometheus exporter 
    metrics:
      prometheus: {} 
    logs:
      access:
        enabled: true